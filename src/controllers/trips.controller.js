import { getTripErrorMessage, getTrips, seedInitialTrips, updateTripDates } from "../services/trip.service.js";

function getRoot() { return document.querySelector(".app-content"); }
function formatDate(value) { if (!value) return "Pendent"; return new Intl.DateTimeFormat("ca-ES", { day: "2-digit", month: "short", year: "numeric" }).format(new Date(`${value}T12:00:00`)); }
function escapeHtml(value = "") { return String(value).replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" }[char])); }

function renderTripRows(trips) {
  return trips.map((trip) => `<article class="trip-management-row" data-trip-id="${trip.id}" data-trip-name="${escapeHtml(trip.name)}" data-start-date="${trip.startDate || ""}" data-end-date="${trip.endDate || ""}" data-closing-date="${trip.closingDate || ""}" data-image-url="${escapeHtml(trip.imageUrl || "")}"><div class="trip-management-row__name"><strong>${escapeHtml(trip.name)}</strong><span class="trip-year-badge">${trip.year || "—"}</span></div><div class="trip-management-row__dates"><span>${trip.startDate ? `${formatDate(trip.startDate)} – ${formatDate(trip.endDate)}` : "Dates pendents"}</span><small>${trip.closingDate ? `Tancament: ${formatDate(trip.closingDate)}` : "Tancament pendent"}</small></div><button class="secondary-button trip-edit-button" type="button" data-edit-trip>Editar</button></article>`).join("");
}

function renderTripsView(trips) {
  return `<section class="trips-management-page"><header class="page-heading"><div><span class="section-kicker">Configuració comercial</span><h1>Etiquetes de viatge</h1><p>Gestiona dates de sortida, tancament comercial i imatge de cada viatge.</p></div></header><section class="trips-management-summary"><div><strong>${trips.length}</strong><span>Etiquetes totals</span></div><div><strong>${trips.filter((trip) => !trip.closingDate).length}</strong><span>Sense data de tancament</span></div></section><section class="trips-management-card"><div class="trips-management-head"><span>Viatge</span><span>Dates i tancament</span><span></span></div><div>${renderTripRows(trips)}</div></section></section>`;
}

function renderEditForm(row) {
  return `<div class="trip-date-modal is-open" id="tripDateModal"><button class="trip-date-modal__backdrop" type="button" data-close-trip-modal></button><section class="trip-date-panel" role="dialog" aria-modal="true"><button class="trip-date-panel__close" type="button" data-close-trip-modal>×</button><span class="section-kicker">Editar viatge</span><h2>${escapeHtml(row.dataset.tripName)}</h2><form id="tripDateForm" data-trip-id="${row.dataset.tripId}"><label class="form-field"><span>Data d'inici</span><input name="startDate" type="date" value="${row.dataset.startDate}" required></label><label class="form-field"><span>Data de finalització</span><input name="endDate" type="date" value="${row.dataset.endDate}" required></label><label class="form-field"><span>Data de tancament comercial</span><input name="closingDate" type="date" value="${row.dataset.closingDate}"><small>A partir d'aquesta data es generarà l'acció «Viatge tancat».</small></label><label class="form-field"><span>URL de la imatge</span><input name="imageUrl" type="url" value="${escapeHtml(row.dataset.imageUrl)}"></label><div class="quick-lead-form__actions"><button class="secondary-button" type="button" data-close-trip-modal>Cancel·lar</button><button class="primary-button primary-button--compact" type="submit">Guardar</button></div><p class="quick-lead-form__message" id="tripDateMessage"></p></form></section></div>`;
}

function setTripsActive() { document.querySelectorAll(".sidebar-nav__item").forEach((button) => button.classList.toggle("is-active", button.textContent.trim().startsWith("Viatges"))); }
async function showTripsView() { const root = getRoot(); if (!root) return; root.innerHTML = '<section class="trips-management-page"><div class="leads-loading"><span class="leads-loading__spinner"></span><p>Preparant viatges...</p></div></section>'; try { await seedInitialTrips(); root.innerHTML = renderTripsView(await getTrips()); } catch (error) { root.innerHTML = `<section class="trips-management-page"><div class="leads-error">${getTripErrorMessage(error)}</div></section>`; } }
function enableTripsButton() { const button = [...document.querySelectorAll(".sidebar-nav__item")].find((item) => item.textContent.trim().startsWith("Viatges")); if (!button) return; button.disabled = false; button.querySelector("small")?.remove(); }
const observer = new MutationObserver(enableTripsButton); observer.observe(document.body, { childList: true, subtree: true }); enableTripsButton();

document.addEventListener("click", (event) => { const navButton = event.target.closest(".sidebar-nav__item"); const editButton = event.target.closest("[data-edit-trip]"); const closeButton = event.target.closest("[data-close-trip-modal]"); if (navButton?.textContent.trim().startsWith("Viatges")) { setTripsActive(); showTripsView(); return; } if (editButton) { document.body.insertAdjacentHTML("beforeend", renderEditForm(editButton.closest("[data-trip-id]"))); return; } if (closeButton) document.querySelector("#tripDateModal")?.remove(); });

document.addEventListener("submit", async (event) => { if (event.target.id !== "tripDateForm") return; event.preventDefault(); const form = event.target; const message = form.querySelector("#tripDateMessage"); try { await updateTripDates(form.dataset.tripId, Object.fromEntries(new FormData(form).entries())); document.querySelector("#tripDateModal")?.remove(); await showTripsView(); window.dispatchEvent(new CustomEvent("travelflow:tasks-updated")); } catch (error) { message.classList.add("is-error"); message.textContent = getTripErrorMessage(error); } });